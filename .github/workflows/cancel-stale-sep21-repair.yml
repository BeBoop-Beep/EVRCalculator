name: Cancel Stale Sep21 Repair

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/cancel-stale-sep21-repair.yml'

permissions:
  actions: write
  contents: read

jobs:
  cancel:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Cancel stale repair holding the self-hosted runner
        shell: bash
        run: |
          set -euo pipefail
          RUN_ID=35757588427
          CODE="$(curl --silent --show-error -o /tmp/cancel.txt -w '%{http_code}' -X POST             -H "Authorization: Bearer $GH_TOKEN"             -H "Accept: application/vnd.github+json"             -H "X-GitHub-Api-Version: 2022-11-28"             "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/runs/$RUN_ID/cancel" || true)"
          echo "cancel_run=$RUN_ID http=$CODE body=$(cat /tmp/cancel.txt 2>/dev/null || true)"
          if [ "$CODE" != "202" ] && [ "$CODE" != "409" ]; then
            exit 1
          fi
