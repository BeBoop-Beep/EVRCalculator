name: Market Quality Timeout Validation

on:
  push:
    branches: [fix/market-quality-timeout-resilience-20260922]
    paths:
      - '.github/workflows/market-quality-timeout-validation.yml'
  pull_request:
    branches: [main]
    paths:
      - 'backend/db/services/market_date_quality.py'
      - 'backend/tests/unit/db/services/test_market_date_quality_lightweight_resolver.py'
      - '.github/workflows/market-quality-timeout-validation.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      SUPABASE_URL: https://example.supabase.co
      SUPABASE_SERVICE_ROLE_KEY: ci-placeholder-service-role-key
      SUPABASE_ANON_KEY: ci-placeholder-anon-key
      JWT_SECRET: ci-placeholder-jwt
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: python -m pip install -q pytest supabase python-dotenv
      - run: python -m pytest -q backend/tests/unit/db/services/test_market_date_quality_lightweight_resolver.py backend/tests/unit/db/services/test_market_publication_gate.py
