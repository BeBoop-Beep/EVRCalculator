import Link from "next/link";
import InfoPopover from "@/components/ui/InfoPopover";
import { formatPublicRipScore } from "@/constants/exploreRankingConfig";
import { getRipTierPresentation, publicLeaderScoreTier } from "@/components/explore/ripTierPresentation.mjs";
import { finite, formatStrength, pluralFamilyLabel } from "./productDetailModel.mjs";

const money = (value) => finite(value) === null ? "Unavailable" : finite(value).toLocaleString("en-US", { style: "currency", currency: "USD" });
const percent = (value) => finite(value) === null ? "Unavailable" : `${(finite(value) * 100).toFixed(1).replace(/\.0$/, "")}%`;

export function ProductRipLock() {
  return <section data-product-rip-lock aria-labelledby="product-rip-lock-title" className="set-glass-surface relative min-h-48 overflow-hidden rounded-2xl border border-amber-300/20"><div aria-hidden="true" className="absolute inset-0 grid grid-cols-3 gap-3 p-5 opacity-20 blur-sm"><span className="rounded-xl bg-white/10" /><span className="rounded-xl bg-white/10" /><span className="rounded-xl bg-white/10" /></div><div className="relative z-10 flex min-h-48 flex-col items-center justify-center bg-[rgba(2,6,23,.68)] px-5 py-7 text-center"><p className="text-xs font-bold uppercase tracking-[.14em] text-amber-300">🔒 Index Plus</p><h2 id="product-rip-lock-title" className="mt-2 text-2xl font-semibold">Unlock Product RIP</h2><p className="mt-2 max-w-xl text-sm text-[var(--text-secondary)]">See how this product ranks within its format and what its modeled opening outcomes look like.</p><Link href="/pricing" className="mt-4 inline-flex min-h-11 items-center rounded-lg border border-amber-300/40 bg-amber-300/10 px-4 text-sm font-semibold text-amber-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-300">Explore Index Plus</Link></div></section>;
}

function ScoreCard({ label, value, tier: tierValue, info, primary = false, children }) {
  const tier = getRipTierPresentation(tierValue, { strength: primary ? "hero" : "supporting" });
  return <div data-product-rip-score={label.toLowerCase().replaceAll(" ", "-")} style={tier.style} className={`relative overflow-hidden rounded-xl border border-[var(--tier-border)] bg-[var(--tier-surface)] p-4 ${primary ? "sm:p-5" : ""}`}><div aria-hidden="true" className="absolute inset-x-0 top-0 h-px bg-[var(--tier-color)] opacity-75" /><div className="flex items-start justify-between gap-3"><dt className="flex items-center gap-1.5 text-xs font-semibold uppercase tracking-[.09em] text-[var(--text-secondary)]"><span>{label}</span>{info ? <InfoPopover text={info} /> : null}</dt><span className="rounded-full border border-[var(--tier-border)] bg-black/15 px-2.5 py-1 text-xs font-bold text-[var(--tier-color)]">{tier.label}</span></div><dd className={`mt-4 font-semibold leading-none tabular-nums ${primary ? "text-4xl sm:text-5xl" : "text-3xl"}`}>{finite(value) === null ? "Unavailable" : <>{formatPublicRipScore(value)} <span className="text-xs font-medium text-[var(--text-secondary)]">/10</span></>}</dd>{children}</div>;
}

export function ProductRipSection({ detail }) {
  const { rip, product } = detail;
  if (!rip.available) return <section data-product-rip-unavailable className="set-glass-surface rounded-2xl border p-5"><p className="text-xs font-bold uppercase tracking-[.14em] text-[var(--accent)]">Product RIP</p><h2 className="mt-1 text-2xl font-semibold">Opening intelligence is not currently available for this product.</h2><p className="mt-2 text-sm text-[var(--text-secondary)]">This product does not have a current published, format-comparable opening result. Market and catalog information remain available.</p></section>;
  const financialTier = publicLeaderScoreTier(rip.financialRipLeaderScore);
  const collectorTier = publicLeaderScoreTier(rip.collectorAppealScore);
  return <section data-product-rip-section aria-labelledby="product-rip-title" className="set-glass-surface rounded-2xl border p-4 sm:p-5"><p className="text-xs font-bold uppercase tracking-[.14em] text-[var(--accent)]">Product RIP · Index Plus</p><h2 id="product-rip-title" className="mt-1 text-2xl font-semibold">How favorable is opening this exact product?</h2><p className="mt-1.5 max-w-3xl text-sm text-[var(--text-secondary)]">Product RIP measures this product and ranks it only against comparable products of the same format.</p><dl className="mt-4 grid gap-3"><ScoreCard primary label="Overall RIP" value={rip.overallRipLeaderScore} tier={rip.publicTier}><div className="mt-5 flex flex-wrap items-center gap-x-5 gap-y-2 border-t border-[var(--tier-border)] pt-4"><p className="text-sm font-semibold">{formatStrength(rip)}</p><p className="text-xs text-[var(--text-secondary)]"><span className="font-semibold text-[var(--text-primary)]">Format Rank</span> · #{rip.familyRank} of {rip.familySize} {pluralFamilyLabel(product.productFamilyLabel)}</p></div></ScoreCard></dl><p data-product-rip-formula className="my-3 text-center text-xs font-medium text-[var(--text-secondary)]">Overall RIP = 90% Financial RIP + 10% Collector Appeal</p><dl className="grid gap-3 sm:grid-cols-2"><ScoreCard label="Financial RIP" value={rip.financialRipLeaderScore} tier={financialTier} /><ScoreCard label="Collector Appeal" value={rip.collectorAppealScore} tier={collectorTier} info="Collector Appeal reflects the collector-facing appeal of the product's parent set. It is not recalculated simply because this product contains more packs." /></dl></section>;
}

export const APPROVED_PRIMARY_OUTCOMES = [
  ["Expected Value", "expectedValue", "Average modeled gross market value across simulated openings.", money],
  ["Typical Opening", "medianValue", "The median modeled result — half of simulated openings finished above it and half below it.", money],
  ["Chance to Recover Cost", "chanceToRecoverCost", "Modeled probability that the opening's gross market value reaches or exceeds the product's current market price.", percent],
  ["Entertainment Cost", "entertainmentCost", "Purchase price minus modeled gross market value.", money],
  ["Realistic Upside", "p95Value", "The 95th-percentile opening value — roughly 5% of modeled openings reached this value or higher.", money],
  ["Jackpot Upside", "p99Value", "The 99th-percentile opening value — roughly 1% of modeled openings reached this value or higher.", money],
];

function OutcomeRangeRail({ rip, currentPrice }) {
  const markers = [["P05", rip.p05Value, "percentile"], ["Typical", rip.medianValue, "percentile"], ["EV", rip.expectedValue, "average"], ["Price", currentPrice, "price"], ["P95", rip.p95Value, "percentile"], ["P99", rip.p99Value, "percentile"]].map(([label, raw, kind]) => ({ label, value: finite(raw), kind }));
  const available = markers.filter(({ value }) => value !== null);
  if (!available.length) return null;
  const max = Math.max(...available.map(({ value }) => value), 1);
  return <div data-outcome-range-rail className="mt-4 rounded-xl border border-[var(--border-subtle)] bg-[rgba(2,8,23,.38)] p-4"><div className="flex items-center justify-between gap-3"><h3 className="text-sm font-semibold">Outcome Range</h3><span className="text-[11px] text-[var(--text-secondary)]">Positioned by modeled value</span></div><div className="relative mx-1 mt-5 h-3 rounded-full border border-white/10 bg-[linear-gradient(90deg,rgba(248,113,113,.16),rgba(56,189,248,.2),rgba(167,139,250,.22))]">{available.map((marker) => <span key={marker.label} data-outcome-marker={marker.label.toLowerCase()} aria-label={`${marker.label}: ${money(marker.value)}`} className={`absolute top-1/2 block -translate-x-1/2 -translate-y-1/2 border-2 ${marker.kind === "price" ? "h-4 w-4 rotate-45 rounded-[3px] border-amber-200 bg-amber-400" : "h-3 w-3 rounded-full border-slate-950 bg-[var(--accent)]"}`} style={{ left: `${Math.max(1, Math.min(99, (marker.value / max) * 100))}%` }} />)}</div><dl className="mt-4 grid grid-cols-3 gap-x-3 gap-y-2 sm:grid-cols-6">{markers.map((marker) => <div key={marker.label} className={marker.kind === "price" ? "text-amber-200" : ""}><dt className="text-[10px] font-bold uppercase tracking-[.08em]">{marker.label}</dt><dd className="mt-0.5 text-xs font-semibold tabular-nums">{money(marker.value)}</dd></div>)}</dl></div>;
}

export function ProductOpeningProfile({ rip, currentPrice }) {
  const value = (key) => key === "entertainmentCost" ? rip.entertainmentCost?.entertainmentCost : rip[key];
  const composition = rip.composition || {};
  return <section data-opening-outcome-profile aria-labelledby="opening-profile-title" className="set-glass-surface rounded-2xl border p-4 sm:p-5"><p className="text-xs font-bold uppercase tracking-[.14em] text-[var(--accent)]">Index Plus</p><h2 id="opening-profile-title" className="mt-1 text-2xl font-semibold">Opening Outcome Profile</h2><p className="mt-1.5 text-sm text-[var(--text-secondary)]">What does opening this product actually look like?</p><OutcomeRangeRail rip={rip} currentPrice={currentPrice} /><dl data-primary-outcome-metrics className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{APPROVED_PRIMARY_OUTCOMES.map(([label, key, info, formatter]) => <div data-primary-outcome={key} key={key} className="rounded-xl border border-[var(--border-subtle)] bg-[rgba(2,8,23,.38)] p-4"><dt className="flex items-center gap-1.5 text-xs font-semibold uppercase tracking-[.07em] text-[var(--text-secondary)]">{label}<InfoPopover text={info} /></dt><dd className="mt-2 text-xl font-semibold tabular-nums">{formatter(value(key))}</dd></div>)}</dl><div className="mt-4 rounded-xl border border-[var(--border-subtle)] bg-white/[.02] p-4"><h3 className="text-sm font-semibold">Product Composition</h3><dl className="mt-3 grid gap-3 text-sm sm:grid-cols-2 lg:grid-cols-4"><div><dt className="text-[var(--text-secondary)]">Packs</dt><dd className="font-semibold">{finite(composition.packCount) ?? "Unavailable"}</dd></div><div><dt className="text-[var(--text-secondary)]">Random Packs</dt><dd className="font-semibold">{finite(composition.randomPackCount) ?? "Unavailable"}</dd></div><div><dt className="text-[var(--text-secondary)]">Modeled Guaranteed Components</dt><dd className="font-semibold">{finite(composition.guaranteedComponentCount) ?? "Unavailable"}</dd></div><div><dt className="text-[var(--text-secondary)]">Guaranteed Component Value</dt><dd className="font-semibold">{money(composition.guaranteedComponentMarketValue)}</dd></div></dl><p className="mt-3 text-xs text-[var(--text-secondary)]">Accessories are not assigned modeled market value under the current opening model.</p></div><details className="mt-3 rounded-xl border border-[var(--border-subtle)] p-4 text-sm text-[var(--text-secondary)]"><summary className="cursor-pointer font-semibold text-[var(--text-primary)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)]">Additional outcome details</summary><dl className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><div><dt>P05 Opening</dt><dd className="font-semibold text-[var(--text-primary)]">{money(rip.p05Value)}</dd></div><div><dt>Expected Loss When Losing</dt><dd className="font-semibold text-[var(--text-primary)]">{money(rip.expectedLossWhenLosing)}</dd></div><div><dt>Median Loss When Losing</dt><dd className="font-semibold text-[var(--text-primary)]">{money(rip.medianLossWhenLosing)}</dd></div><div><dt>Modeled Return Ratio</dt><dd className="font-semibold text-[var(--text-primary)]">{finite(rip.totalValueToCostRatio) === null ? "Unavailable" : `${(rip.totalValueToCostRatio * 100).toFixed(1)}%`}</dd></div><div><dt>Entertainment Cost Per Pack</dt><dd className="font-semibold text-[var(--text-primary)]">{money(rip.entertainmentCost?.entertainmentCostPerPackEquivalent)}</dd></div></dl><p className="mt-4">Entertainment Cost uses gross modeled market value. Marketplace fees, shipping, liquidation friction, bid/ask spread, and grading are not deducted or assumed. It is the price of the modeled opening experience, not a loss prediction.</p></details></section>;
}
