import RoutePageShell from "@/components/Profile/RoutePageShell";

export default function MyCollectionWishlistPage() {
  return (
    <RoutePageShell
      eyebrow="Manage Wishlist"
      title="My Wishlist"
      subtitle="Track targets, priorities, and acquisition plans."
    >
      <div className="rounded-xl border border-[var(--border-subtle)] bg-[var(--surface-panel)] p-5 text-sm text-[var(--text-secondary)]">
        Wishlist management workspace placeholder.
      </div>
    </RoutePageShell>
  );
}
